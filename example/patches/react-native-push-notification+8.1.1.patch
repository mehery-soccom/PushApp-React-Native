diff --git a/node_modules/react-native-push-notification/android/src/main/java/com/dieam/reactnativepushnotification/modules/RNPushNotificationActions.java b/node_modules/react-native-push-notification/android/src/main/java/com/dieam/reactnativepushnotification/modules/RNPushNotificationActions.java
index ada9603..85402bc 100644
--- a/node_modules/react-native-push-notification/android/src/main/java/com/dieam/reactnativepushnotification/modules/RNPushNotificationActions.java
+++ b/node_modules/react-native-push-notification/android/src/main/java/com/dieam/reactnativepushnotification/modules/RNPushNotificationActions.java
@@ -23,7 +23,7 @@ public class RNPushNotificationActions extends BroadcastReceiver {
     public void onReceive(final Context context, Intent intent) {
       String intentActionPrefix = context.getPackageName() + ".ACTION_";
 
-      Log.i(LOG_TAG, "RNPushNotificationBootEventReceiver loading scheduled notifications");
+      Log.i(LOG_TAG, "RNPushNotificationActions onReceive action=" + intent.getAction());
 
       if (null == intent.getAction() || !intent.getAction().startsWith(intentActionPrefix)) {
         return;
@@ -55,6 +55,12 @@ public class RNPushNotificationActions extends BroadcastReceiver {
       }
 
       boolean invokeApp = bundle.getBoolean("invokeApp", true);
+      String actionLabel = bundle.getString("action");
+      Log.i(
+          LOG_TAG,
+          "notification action tap: invokeApp=" + invokeApp
+              + " action=" + actionLabel
+              + " id=" + bundle.getString("id"));
 
       // Notify the action.
       if(invokeApp) {
diff --git a/node_modules/react-native-push-notification/android/src/main/java/com/dieam/reactnativepushnotification/modules/RNPushNotificationJsDelivery.java b/node_modules/react-native-push-notification/android/src/main/java/com/dieam/reactnativepushnotification/modules/RNPushNotificationJsDelivery.java
index 350758e..afc81de 100644
--- a/node_modules/react-native-push-notification/android/src/main/java/com/dieam/reactnativepushnotification/modules/RNPushNotificationJsDelivery.java
+++ b/node_modules/react-native-push-notification/android/src/main/java/com/dieam/reactnativepushnotification/modules/RNPushNotificationJsDelivery.java
@@ -2,6 +2,7 @@ package com.dieam.reactnativepushnotification.modules;
 
 import android.os.Build;
 import android.os.Bundle;
+import android.util.Log;
 
 import com.facebook.react.bridge.Arguments;
 import com.facebook.react.bridge.ReactContext;
@@ -18,6 +19,8 @@ import java.util.Set;
  */
 
 public class RNPushNotificationJsDelivery {
+    private static final String TAG = "RNPushNotifJsDelivery";
+
     private ReactContext mReactContext;
 
     public RNPushNotificationJsDelivery(ReactContext reactContext) {
@@ -25,10 +28,20 @@ public class RNPushNotificationJsDelivery {
     }
 
     void sendEvent(String eventName, Object params) {
-        if (mReactContext.hasActiveCatalystInstance()) {
+        // Prefer hasActiveReactInstance() — on modern RN / bridgeless, the deprecated
+        // hasActiveCatalystInstance path can block emits so JS never sees action taps.
+        boolean ready = mReactContext.hasActiveReactInstance();
+        if (!ready) {
+            Log.w(TAG, "skip emit (no active react instance): " + eventName);
+            return;
+        }
+        try {
             mReactContext
                     .getJSModule(DeviceEventManagerModule.RCTDeviceEventEmitter.class)
                     .emit(eventName, params);
+            Log.i(TAG, "emit to JS: " + eventName);
+        } catch (Exception e) {
+            Log.e(TAG, "emit failed: " + eventName, e);
         }
     }
 
